# PrintEdge Audit Report — 2026-05-24

**Environment:** Local (`venv`, SQLite default)  
**Automated checks:** `python manage.py run_qa_checks` — **33/33 PASS**

---

## Phase 1: Branding

| Item | Status |
|------|--------|
| New static logo (`static/icons/logo.png`) | Done — text mark, transparent PNG |
| Favicon + PWA icons (192, 512) | Done |
| Removed admin logo upload UI | Done |
| All templates use static logo, alt `PrintEdge` | Done |
| Legacy `SiteSettings.logo` cleared via `clear_branding_logos` | Done |
| PWA manifest name → PrintEdge | Done |
| Display board shows logo image | Done |

---

## Phase 2: Functional QA

| Area | Result |
|------|--------|
| Public pages (/, pricing, contact, auth) | PASS (automated 200) |
| Anonymous protected routes redirect | PASS |
| Pricing double-sided (11 pp B&W=18, color=48) | PASS |
| Admin dashboard (super_admin) | PASS |
| Customer dashboard | PASS |
| Unverified customer blocked from new order | PASS |
| Profile avatar upload POST | PASS (no 500) |
| IDOR order detail | Skipped (no cross-customer orders in DB) |

**Manual retest recommended:** Full new-order PDF upload flow, walk-in POS with/without file, responsive breakpoints 320–1440px.

---

## Phase 3: Security

| Control | Status |
|---------|--------|
| Auth rate limiting | Present (`core/ratelimit.py`) |
| Magic-byte upload validation | Present |
| Double-extension / null-byte filename reject | Added (`core/utils.py`) |
| Randomized order file storage names | Added (`secure_storage_name`) |
| Email verification before online orders | Re-introduced (`is_email_verified`) |
| Walk-in POS permission gate | `@permission_required('walkin_order')` |
| Viewer readonly on order detail / inventory / services POST | Added |
| Avatar private bucket display | Presigned URLs via `avatar_display_url` |
| CSRF middleware | Default Django |
| Security headers | Verified in QA |
| Customer → admin redirect | PASS |

---

## Phase 4: Local run

```bash
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py clear_branding_logos
python manage.py seed_data
python manage.py seed_qa_users
python manage.py collectstatic --noinput
python manage.py run_qa_checks
python manage.py runserver
```

**Credentials:** `admin@printedge.com` / `Admin@123456` — QA users `qa_*@test.local` / `TestPass123!`

---

## Notes

- Email verification requires `EMAIL_HOST_USER` in `.env` for production; dev users can use verification link from logs or mark verified via admin.
- `SiteSettings.logo` field retained in DB but unused (zero-downtime).
- Open items unchanged: `password_plain` (PE-023), Tailwind CDN (PE-022), full CSP (PE-024).
